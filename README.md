# gestionador-tareas-eval3-frontend
un gestionador de tareas usando manipulacion de DOM para un ejercicio de clase
permite crear y borrar tareas, usando formatos de fecha y texto
es muy simple! al mismo tiempo esta protegido contra xss

